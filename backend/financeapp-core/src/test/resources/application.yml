# Test profile config for integration tests (financeapp-core)
app:
  jwt:
    secret: test-jwt-secret-for-integration-tests-must-be-at-least-256-bits-long
    access-token-expiration: 86400000
    refresh-token-expiration: 604800000

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
